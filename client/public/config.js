// Runtime configuration loaded before the app bundle (see index.html).
//
// Leave values empty to fall back to build-time VITE_* env vars (or local-dev
// defaults). To change the backend URL WITHOUT rebuilding, set them here, e.g.:
//   apiUrl:    'https://<backend-choreo-url>/api'
//   socketUrl: 'https://<backend-choreo-url>'
window.configs = {
  apiUrl: '',
  socketUrl: ''
};
